# HAM OLA Frontend (React + Vite)

This React app is pre-configured to use your Render backend at: https://church-website-optimized.onrender.com

Quick start:

npm install
npm run dev

Build for production:

npm run build
npm run preview
